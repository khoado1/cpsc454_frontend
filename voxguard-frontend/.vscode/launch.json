{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach Chrome (React)",
      "type": "pwa-chrome",
      "request": "attach",
      "port": 9222,
      "urlFilter": "http://localhost:3000/*",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
